{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://houseaccount/providers.schema.json",
  "title": "Pricing-page providers by market",
  "description": "public/providers.json — synthetic service providers keyed by market name, emitted by eval/export_ui.py. Each provider is a pricing option whose quote is price_factor x the model's estimate midpoint.",
  "type": "object",
  "additionalProperties": {
    "type": "array",
    "minItems": 1,
    "items": {
      "type": "object",
      "required": ["name", "rating", "reviews", "price_factor", "eta_days"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "rating": { "type": "number", "minimum": 0, "maximum": 5 },
        "reviews": { "type": "integer", "minimum": 0 },
        "price_factor": { "type": "number", "exclusiveMinimum": 0, "description": "Multiplier applied to the estimate midpoint to get this provider's quote." },
        "eta_days": { "type": "integer", "minimum": 0, "description": "Earliest availability, in days." }
      }
    }
  }
}
